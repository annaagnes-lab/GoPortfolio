{
  "name": "amfi-portfolio-tracker",
  "version": "1.0.1",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-scripts": "5.0.1",
    "serve": "^14.2.4"
  },
  "scripts": {
    "dev": "react-scripts start",
    "build": "react-scripts build",
    "prestart": "npm run build",
    "start": "serve -s build -l 0.0.0.0:${PORT:-3000}",
    "start": "serve -s build -l tcp://0.0.0.0:${PORT:-3000}",
    "build": "react-scripts build",
    "dev": "react-scripts start",
    "test": "react-scripts test --watchAll=false"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
